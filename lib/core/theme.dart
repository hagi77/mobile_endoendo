import 'package:flutter/material.dart';

const textThemes = TextTheme(
  headline1: TextStyle(
      fontSize: 28.0, fontWeight: FontWeight.bold, color: primaryTextColor),
  headline2: TextStyle(
      fontSize: 24.0, fontWeight: FontWeight.bold, color: primaryTextColor),
  headline3: TextStyle(
      fontSize: 18.0, fontWeight: FontWeight.bold, color: primaryTextColor),
  bodyText2: TextStyle(fontSize: 14.0, color: primaryTextColor),
);

const primaryColor = Colors.blue;
const surfaceColor = Colors.white;
const primaryTextColor = Colors.black;
